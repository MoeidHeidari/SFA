#pragma once

/**
 * This is a foo class to demonstrate features of the boiler plate.
 */
class LinearSearch {
	public:

  /**
   * Default constructor for foo (does nothing).
   */
  LinearSearch();
  /**
   * Returns a bool.
   * @return Always True.
   */
  int linearSearchFunc(int* , int , int );
};
